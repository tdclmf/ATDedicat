diff --git a/src/xrGame/inventory_item.cpp b/_recovery_lost/blob_ebf.cpp
index 61b6d202..b0647072 100644
--- a/src/xrGame/inventory_item.cpp
+++ b/_recovery_lost/blob_ebf.cpp
@@ -288,9 +288,9 @@ void CInventoryItem::OnEvent(NET_Packet& P, u16 type)
 	}
 }
 
-//??????? ???????????? ???? ??????????? ? ?????? ????? ???? 
-//? ????????? ? ????????? ??????????????? ?????? ? ????????????
-//???????, ??????? ??????? ?????? ???? ??????????????
+//??????? ???????????? ???? ??????????? ? ?????? ????? ???? 
+//? ????????? ? ????????? ??????????????? ?????? ? ????????????
+//???????, ??????? ??????? ?????? ???? ??????????????
 bool CInventoryItem::Detach(const char* item_section_name, bool b_spawn_item)
 {
 	if (OnClient()) return true;
@@ -370,7 +370,7 @@ void CInventoryItem::net_Destroy()
 		VERIFY(std::find(m_pInventory->m_all.begin(), m_pInventory->m_all.end(), this)==m_pInventory->m_all.end());
 	}
 
-	//????????? ???????? ?? ????????????
+	//????????? ???????? ?? ????????????
 	//.	m_pInventory = NULL;
 }
 
@@ -873,22 +873,8 @@ void CInventoryItem::PH_A_CrPr		()
 		VERIFY( K );
 		if (!object().PPhysicsShell())
 		{
-			// Some replicated inventory items are spawned already parented in inventories and
-			// intentionally don't have physics shell on client. Try to build one only for world items.
-			if (!object().H_Parent())
-				object().setup_physic_shell();
-		}
-		if (!object().PPhysicsShell())
-		{
-			static u32 s_missing_shell_diag_budget = 128;
-			if (s_missing_shell_diag_budget)
-			{
-				--s_missing_shell_diag_budget;
-				Msg("! [NET] CInventoryItem::PH_A_CrPr skip item without physics shell [%s][%d] parent=%u",
-					object().cName().c_str(), object().ID(), object().H_Parent() ? object().H_Parent()->ID() : u16(-1));
-			}
-			m_just_after_spawn = false;
-			object().CrPr_SetActivated(false);
+			Msg("! ERROR: PhysicsShell is NULL, object [%s][%d]", object().cName().c_str(), object().ID());
+			VERIFY2(0, "physical shell is NULL");
 			return;
 		}
 		if(!object().PPhysicsShell()->isFullActive())
@@ -977,7 +963,7 @@ void CInventoryItem::CalculateInterpolationParams()
 		for (u32 k=0; k<3; k++)
 		{
 			P0[k] = c*(c*(c*p->SCoeff[k][0]+p->SCoeff[k][1])+p->SCoeff[k][2])+p->SCoeff[k][3];
-			P1[k] = (c*c*p->SCoeff[k][0]*3+c*p->SCoeff[k][1]*2+p->SCoeff[k][2])/3; // ???????? ?? ??????? ? 3 ???? ????????? ???????? ??? ??????? ????????????? !!!!
+			P1[k] = (c*c*p->SCoeff[k][0]*3+c*p->SCoeff[k][1]*2+p->SCoeff[k][2])/3; // ???????? ?? ??????? ? 3 ???? ????????? ???????? ??? ??????? ????????????? !!!!
 		};
 		P0.set(p->IStartPos);
 		P1.add(p->IStartPos);
@@ -1146,11 +1132,11 @@ void CInventoryItem::Interpolate()
 	net_updateInvData* p = NetSync();
 	CPHSynchronize* pSyncObj = object().PHGetSyncItem(0);
 
-	const bool is_artefact = (object().cast_artefact() != nullptr);
+	CArtefact* artefact = smart_cast<CArtefact*>(this);
 
 	//simple linear interpolation...
 	if (!object().H_Parent() &&
-		(object().getVisible() || is_artefact)&&
+		(object().getVisible() || artefact)&&
 		object().m_pPhysicsShell &&
 		!OnServer() &&
 		p->NET_IItem.size())
@@ -1179,16 +1165,6 @@ void CInventoryItem::Interpolate()
 		pSyncObj->set_State(newState);
 	}
 }
-
-void CInventoryItem::ClearNetInterpolationQueue()
-{
-		net_updateInvData* p = NetSync();
-		if (!p)
-			return;
-
-		p->NET_IItem.clear();
-}
-
 float CInventoryItem::interpolate_states(net_update_IItem const & first, net_update_IItem const & last, SPHNetState & current)
 {
 	float ret_val = 0.f;
@@ -1529,4 +1505,3 @@ bool CInventoryItem::has_network_synchronization() const
 {
 	return false;
 }
-
